spring:
  datasource:
    url: jdbc:h2:file:/Users/luciooliveira/workspace/lets_code/turma_808/todo-list/database
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update

server:
  port: 8080

jwt:
  secret: 'letscode'